<%= form_with(model: sinsi, local: true) do |form| %>
  <% if sinsi.errors.any? %>
    <div id="error_explanation">
      <div class="alert-danger">
        <ul class="validate">
          <% sinsi.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <br />
  <div class="field">
    <%= form.label "タイトル" %><em> (省略可)</em><br />
    <%= form.text_field :title, value: @sinsi.title, :placeholder => "無題" %>
  </div><br />

  <div class="field">
    <%= form.label "ひとこと" %><em> (省略可)</em><br />
    <%= form.text_field :word, value: @sinsi.word, :placeholder => "無言" %>
  </div><br />

  <div class="field">
    <%= form.label "投稿する画像の選択" %><br />
    <%= form.file_field :picture %>
  </div><br />

  <%= form.hidden_field :user_id, value: current_user.id %>

  <div class="actions">
    <%= form.submit data: { confirm: '確定します。よろしいですか？' } %>
  </div>
<% end %>
